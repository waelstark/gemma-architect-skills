import os
import json
import re
import shutil

# 1. دمج المجموعتين في مصفوفة واحدة ضخمة (16 مهارة متكاملة)
skills_data = [
  # --- مجموعة الـ ERP ---
  {
    "name": "erp-module-architect",
    "description": "Design modular ERP architecture with independent but integrated business modules",
    "trigger": "ERP module architecture",
    "instructions": "Design the ERP module system using Modular Monolith pattern. Provide: 1) Module breakdown listing each business module with its single responsibility, 2) Inter-module communication strategy defining how modules share data without direct coupling, using domain events or shared kernel, 3) Module dependency map showing which modules depend on others and in which direction, 4) Module enable/disable mechanism allowing clients to activate only purchased modules, 5) Shared kernel definition listing entities and utilities shared across all modules such as User, Currency, and Audit, 6) Module database strategy defining whether modules share tables or have isolated schemas with clear boundaries, 7) Module versioning approach for updating one module without breaking others, 8) Plugin extension points where clients can add custom logic without modifying core code. Ask for the business domain and expected number of users before starting. Always warn against Microservices for ERP teams under 50 developers."
  },
  {
    "name": "workflow-engine-designer",
    "description": "Design approval workflow engine for ERP business processes",
    "trigger": "workflow approval engine",
    "instructions": "Design the workflow engine for ERP approval processes. Provide: 1) Workflow definition schema showing how to define steps, conditions, and transitions in a configurable format stored in database not code, 2) State machine design listing all possible states for each document type such as Draft, Submitted, Under Review, Approved, Rejected, Cancelled, 3) Approver resolution logic explaining how the system finds the correct approver based on organizational hierarchy, amount thresholds, and department rules, 4) Escalation rules defining what happens when an approver does not act within the configured time limit including auto-escalate and auto-approve options, 5) Delegation mechanism allowing a user to delegate their approval authority to another user during vacation with date range, 6) Parallel approval design for cases requiring simultaneous approval from multiple parties such as Finance and Legal, 7) Conditional routing rules using JSON-based condition engine to route documents differently based on amount, department, or item type, 8) Audit trail per step recording who approved, who rejected, timestamp, IP address, and comment for every workflow action, 9) Notification templates for each workflow event sent via email and in-app notification, 10) Segregation of duties enforcement preventing the same user from both creating and approving the same document."
  },
  {
    "name": "financial-architect",
    "description": "Design double-entry accounting and financial module architecture",
    "trigger": "financial accounting module",
    "instructions": "Design the financial accounting architecture. Provide: 1) Double-entry bookkeeping engine ensuring every transaction creates balanced journal entries where total debits equal total credits, with validation that rejects unbalanced entries, 2) Chart of accounts structure with account types Assets, Liabilities, Equity, Revenue, Expense, each with code ranges, parent-child hierarchy, and whether the account allows direct posting or is a summary account, 3) Accounting period management covering fiscal year definition, period open and close controls, preventing posting to closed periods, and year-end closing process that transfers net income to retained earnings, 4) Journal entry schema with header containing date, reference, description, currency, and lines containing account code, debit amount, credit amount, cost center, and project, 5) Multi-currency handling covering exchange rate table with daily rates, functional currency definition, transaction currency recording, currency revaluation at period end, and realized versus unrealized gains and losses, 6) Tax engine design for VAT calculation including tax codes, rates, tax groups, input tax, output tax, and tax return report generation, 7) Cost center accounting for allocating revenues and expenses to departments or projects with cost center hierarchy, 8) Budget control module with budget definition per account and period, commitment tracking when purchase orders are created, and budget availability check before allowing transactions, 9) Financial reports definition covering Trial Balance, Balance Sheet, Profit and Loss, and Cash Flow Statement with drill-down to source transactions, 10) Period closing checklist with automated checks before allowing period close such as unposted entries, unreconciled accounts, and pending approvals."
  },
  {
    "name": "rbac-advanced-architect",
    "description": "Design three-level role-based access control for ERP systems",
    "trigger": "ERP permissions access control",
    "instructions": "Design the three-level RBAC system for ERP. Provide: 1) Module-level access defining which roles can access which ERP modules with a role-module permission matrix, 2) Action-level permissions for each module covering Create, Read, Update, Delete, Approve, Export, Print as separate permissions that can be granted independently, 3) Row-level security design ensuring users only see data belonging to their assigned scope such as own records only, own department, own region, or all records, with the implementation approach using database views or query filters, 4) Field-level security defining which fields are visible or editable per role such as salary fields visible only to HR managers, 5) Organizational hierarchy integration where a manager automatically inherits view access to all data of their direct and indirect reports, 6) Role definition schema including role name, description, list of permission codes, scope definition, and whether the role can be assigned to external users, 7) Permission inheritance rules defining how roles stack when a user has multiple roles, using additive permissions where broader scope wins, 8) Segregation of duties conflict matrix listing pairs of permissions that cannot be held by the same user such as Create Purchase Order and Approve Purchase Order, 9) Delegation and proxy access allowing a user to grant another user their permissions temporarily with full audit trail, 10) Permission audit report showing which users can access which data and actions for compliance review."
  },
  {
    "name": "audit-compliance-architect",
    "description": "Design immutable audit trail and compliance architecture for ERP",
    "trigger": "audit trail compliance logging",
    "instructions": "Design the audit and compliance architecture for ERP. Provide: 1) Audit log schema capturing table name, record ID, action type as INSERT UPDATE DELETE APPROVE, changed fields as JSON with old and new values, user ID, user name, IP address, device information, timestamp with timezone, and business reason or approval comment, 2) Implementation strategy for automatically capturing audit logs without requiring developers to write logging code in every service, using database triggers or ORM interceptors, 3) Immutability guarantee ensuring audit logs can never be modified or deleted by any user including system administrators, using append-only storage or cryptographic chaining, 4) Audit log retention policy defining how long to keep logs online for quick access versus archived for compliance, typically 2 years online and 7 years archived, 5) Document change history UI showing a user the complete history of any ERP document with who changed what and when in a readable timeline format, 6) Sensitive data masking in audit logs for fields like passwords, bank account numbers, and national IDs showing only partial values, 7) Compliance report templates for common regulatory requirements showing user access patterns, data changes, and approval audit trails, 8) Suspicious activity detection rules flagging unusual patterns like bulk data export, repeated failed login attempts, or access outside business hours, 9) Data retention and deletion policy for GDPR compliance defining how to handle personal data deletion requests while preserving financial audit requirements, 10) Audit log integrity verification mechanism to prove logs have not been tampered with."
  },
  {
    "name": "master-data-architect",
    "description": "Design master data management for ERP entities",
    "trigger": "master data management",
    "instructions": "Design the master data management architecture. Provide: 1) Master data entities identification listing Customer, Vendor, Employee, Item, Account, Currency, Unit of Measure, and Tax Code as the core master data requiring governance, 2) Master data creation workflow requiring approval before a new customer or vendor is activated, preventing duplicates and ensuring data quality, 3) Duplicate detection rules for each entity type such as matching on Tax ID for companies, phone number for individuals, and IBAN for vendor bank accounts, with merge strategy for confirmed duplicates, 4) Data quality rules defining mandatory fields, format validations, and cross-field validations per entity with error messages in the user language, 5) Customer master schema with segments for basic info, contact info, financial settings including payment terms and credit limit, tax settings, assigned salesperson, and shipping addresses as a separate child table, 6) Vendor master schema with segments for basic info, bank accounts as child table for payment, payment terms, tax registration, approved product categories, and vendor performance rating, 7) Item master schema with segments for description in multiple languages, unit of measure with conversion factors, costing method, inventory settings including reorder point and safety stock, sales pricing tiers, and purchase information, 8) Data ownership definition assigning each master data type to a responsible department such as Finance owns Account master and Procurement owns Vendor master, 9) Master data synchronization strategy if the same master data must exist in multiple systems defining which system is the source of truth, 10) Master data archive and deactivation rules for entities that are no longer used but cannot be deleted due to historical transactions."
  },
  {
    "name": "integration-hub-architect",
    "description": "Design ERP integration architecture with external systems",
    "trigger": "ERP integration external systems",
    "instructions": "Design the ERP integration hub architecture. Provide: 1) Integration patterns selection recommending synchronous REST API for real-time queries, asynchronous message queue for transactions, and file-based EDI for legacy systems, with decision criteria for choosing each, 2) API gateway design as the single entry point for all external systems with authentication, rate limiting, logging, and request routing, 3) Message queue design for outbound integrations using event-driven pattern where ERP publishes events like InvoiceCreated and PurchaseOrderApproved that external systems subscribe to, 4) ZATCA e-Invoice integration architecture for Saudi Arabia covering invoice signing with cryptographic certificate, real-time submission for B2B invoices above threshold, QR code generation for B2C invoices, and clearance status tracking, 5) Banking integration design for bank statement import in MT940 or CSV format, automated bank reconciliation matching bank lines to accounting entries, and payment file generation in bank-required format, 6) Error handling and retry strategy for failed integrations with dead letter queue for messages that fail after maximum retries and alert to operations team, 7) Integration monitoring dashboard showing status of each integration point, recent transaction counts, error rates, and last successful sync time, 8) Data transformation layer design for mapping ERP data structures to external system formats with configurable mapping rules stored in database, 9) Webhook management for notifying external systems of ERP events with delivery confirmation, retry logic, and webhook secret validation, 10) Integration audit log recording every data exchange including timestamp, direction, payload summary, and success or failure status for compliance and debugging."
  },
  {
    "name": "inventory-warehouse-architect",
    "description": "Design inventory management and warehouse architecture for ERP",
    "trigger": "inventory warehouse stock management",
    "instructions": "Design the inventory and warehouse management architecture. Provide: 1) Costing method implementation for FIFO where each receipt batch is tracked separately and oldest batch is consumed first, AVCO where moving average cost is recalculated on every receipt, and Standard Cost where variance between actual and standard is posted to separate variance accounts, 2) Stock transaction types covering Goods Receipt from purchase order, Goods Issue to production or customer, Transfer between warehouses, Inventory Adjustment with mandatory reason and approval, and Return handling both from customers and to vendors, 3) Lot and serial number tracking schema capturing batch number or serial number on every stock movement, expiry date for perishable items, and complete traceability from vendor receipt to customer delivery, 4) Multi-warehouse structure with warehouse definition, zones or aisles within warehouse, bin locations, and bin capacity tracking, 5) Reorder and replenishment logic checking stock levels against reorder point and safety stock, automatically creating purchase requisitions when stock falls below threshold, 6) Physical inventory and cycle count process design covering count sheet generation, blind count entry where counters cannot see system quantities, variance approval workflow for large discrepancies, and posting of approved adjustments, 7) Stock valuation report showing current quantity and value per item per warehouse using the configured costing method, 8) Inventory aging report showing how long current stock has been sitting to identify slow-moving or obsolete items, 9) Stock reservation mechanism blocking quantity for confirmed sales orders before shipment, 10) Quality control integration for items requiring inspection on receipt before stock is available for use."
  },
  {
    "name": "batch-job-architect",
    "description": "Design batch processing architecture for large ERP operations",
    "trigger": "batch processing scheduled jobs",
    "instructions": "Design the batch processing architecture for ERP. Provide: 1) Job definition schema covering job name, description, job class or function, parameters, schedule using cron expression, timeout limit, retry policy, and notification recipients on success and failure, 2) Job execution engine with persistent job queue, worker pool design, priority levels for critical jobs like payroll over reports, and resource throttling to prevent jobs from consuming all server resources, 3) Progress tracking design allowing users to see percentage complete for long-running jobs with estimated time remaining and ability to view live log output, 4) Idempotency design ensuring that if a job fails midway and is restarted it does not create duplicate records or double-post transactions, using checkpoint pattern to record last successful step, 5) Payroll batch process breakdown covering attendance data collection, gross pay calculation, deduction calculation, net pay computation, payslip generation, accounting entry posting, and bank transfer file generation as sequential steps with rollback if any step fails, 6) Month-end close batch covering depreciation calculation, accruals posting, recurring journal posting, currency revaluation, intercompany elimination, and period close validation checks, 7) Locking mechanism preventing the same job from running twice simultaneously and preventing conflicting jobs from running at the same time such as payroll and period close, 8) Job dependency chain design for jobs that must run in sequence such as stock valuation must complete before cost of goods sold calculation, 9) Large data processing strategy using cursor-based pagination or batch commits every N records to prevent memory overflow and transaction timeout, 10) Job history and performance tracking storing execution time, record count, and resource usage for each run to identify degrading performance over time."
  },
  {
    "name": "erp-reporting-architect",
    "description": "Design reporting and business intelligence architecture for ERP",
    "trigger": "ERP reporting analytics dashboard",
    "instructions": "Design the ERP reporting and BI architecture. Provide: 1) Report tier classification separating operational reports running against live OLTP database for current day data, management reports running against a refreshed data mart for weekly and monthly analysis, and executive dashboards with pre-aggregated KPIs updated on a schedule, 2) Data warehouse design using star schema with fact tables for Transactions, Sales, Purchases, and Inventory Movements, and dimension tables for Date, Customer, Vendor, Item, Account, and Organization, 3) ETL pipeline design extracting data from OLTP database, transforming to warehouse schema, and loading on a configurable schedule such as hourly for operational or nightly for historical, 4) Report definition framework allowing reports to be defined with parameters, filters, grouping options, and output columns without code changes, 5) Financial statement engine for Balance Sheet, Profit and Loss, and Cash Flow using account mapping rules that assign accounts to line items on each statement, with comparative periods and variance columns, 6) Dashboard definition with KPI cards showing current value versus target and trend, charts for time-series data, and drill-down capability from summary to detail to source transaction, 7) Report scheduling and distribution allowing users to subscribe to reports delivered by email as PDF or Excel at configured intervals, 8) Ad-hoc query builder allowing power users to build custom reports by selecting fields from a simplified data catalog without writing SQL, 9) Export architecture supporting PDF for formal reports with company letterhead, Excel for data analysis, and CSV for data exchange, 10) Report performance optimization using database indexes, materialized views for expensive aggregations, and query result caching for reports with identical parameters run multiple times."
  },
  {
    "name": "erp-localization-architect",
    "description": "Design ERP localization for Saudi Arabia and Arabic language requirements",
    "trigger": "ERP Saudi Arabia localization Arabic",
    "instructions": "Design the ERP localization architecture for Saudi Arabia. Provide: 1) ZATCA e-Invoice compliance covering Phase 1 QR code generation for simplified invoices, Phase 2 real-time integration for standard invoices above threshold, XML format according to UBL 2.1 standard, cryptographic signing with certificate issued by ZATCA, UUID generation for each invoice, and clearance and reporting API integration, 2) VAT implementation covering 15 percent standard rate, zero-rated categories for exports and specific goods, exempt categories, input tax recovery rules, VAT return calculation covering output tax minus deductible input tax, and VAT return report in format accepted by ZATCA, 3) Arabic language support covering right-to-left UI layout for all screens, Arabic and English fields stored separately for all master data names and descriptions, Arabic number formatting, Arabic date display with Gregorian and Hijri calendar support and conversion, 4) Hijri calendar implementation for date entry and display using Umm Al-Qura calendar as official Saudi standard, storing dates in Gregorian internally and converting for display, with fiscal year option to follow Hijri year, 5) GOSI integration for social insurance contribution calculation using correct percentage for Saudi and non-Saudi employees, monthly contribution report generation, and file format for GOSI portal submission, 6) Wage Protection System compliance generating WPS file in format required by Ministry of Human Resources for payroll payment verification, 7) Zakat and income tax calculation for Saudi companies covering zakat base calculation, add-backs and deductions per GAZT rules, and annual zakat return schedule preparation, 8) Saudi payroll specifics including housing allowance, transportation allowance, end of service benefit calculation using Saudi Labor Law formula, annual leave encashment, and Iqama-related allowances, 9) Multi-currency with SAR as functional currency, exchange rates from Saudi Central Bank, and revaluation of foreign currency balances at period end, 10) Localized financial reports in Arabic including Chart of Accounts compliant with Saudi accounting standards and financial statements formatted per SOCPA requirements."
  },
  # --- مجموعة الجودة والأداء ---
  {
    "name": "performance-optimizer",
    "description": "Analyze and optimize application performance across all layers",
    "trigger": "performance optimization slow response",
    "instructions": "Analyze and optimize performance across all layers. Provide: 1) Database query analysis covering missing indexes identification by examining queries without index usage, N+1 query problem detection where a query runs inside a loop, and rewriting slow queries using EXPLAIN ANALYZE output, 2) Index strategy design recommending composite indexes for multi-column WHERE clauses, partial indexes for filtered queries, and covering indexes to avoid table lookups, with warning about over-indexing degrading write performance, 3) Connection pooling configuration for database connections defining pool size based on CPU cores and expected concurrency, idle connection timeout, and connection validation, 4) API response time optimization covering response compression with gzip, pagination for large datasets using cursor-based pagination instead of offset for large tables, field selection allowing clients to request only needed fields, and HTTP caching headers for semi-static data, 5) Frontend performance covering bundle size analysis and code splitting to load only required JavaScript per route, image optimization with WebP format and lazy loading, critical CSS extraction for above-fold content, and service worker for asset caching, 6) Caching opportunities analysis identifying data that changes infrequently and is read frequently as caching candidates, with recommended TTL per data type, 7) Background processing recommendations identifying operations that do not need to complete before API response such as sending emails, generating reports, and updating statistics, moving them to background job queue, 8) Memory leak detection patterns in common frameworks and how to profile memory usage, 9) Database write optimization covering batch inserts instead of single-row inserts, write-ahead logging benefits, and read replica routing for heavy read queries, 10) Performance monitoring setup with key metrics to track including p50 p95 p99 response times, database query times, cache hit ratio, and error rates with alerting thresholds."
  },
  {
    "name": "error-logging-architect",
    "description": "Design global error handling, logging, and monitoring architecture",
    "trigger": "error handling logging monitoring",
    "instructions": "Design the error handling and logging architecture. Provide: 1) Error classification system separating operational errors that are expected such as validation failures and not found, from programmer errors that indicate bugs such as null pointer and type errors, with different handling strategy for each type, 2) Global error handler design that intercepts all unhandled errors at the application boundary, formats them consistently, logs them with full context, and returns appropriate response to the client without exposing internal details, 3) Error response format standard with fields for error code as machine-readable string, message as human-readable text in user language, details array for validation errors with field name and constraint, request ID for support tracing, and timestamp, 4) Structured logging format using JSON with fields for level, timestamp, service name, request ID, user ID, action, duration, and message, enabling log aggregation and search, 5) Log levels definition and usage rules where DEBUG is for development only, INFO is for significant business events like order created, WARN is for recoverable issues, ERROR is for failures requiring attention, and FATAL is for system-threatening issues, 6) Correlation ID propagation injecting a unique request ID at the entry point and passing it through all service calls and log entries to trace a complete request across microservices or modules, 7) Log aggregation architecture using ELK Stack or similar to collect logs from all services into central searchable store with retention policy, 8) Alerting rules definition for conditions requiring immediate attention such as error rate exceeding threshold, specific critical error codes occurring, or no logs received for expected period indicating service down, 9) Error budget and SLO definition establishing acceptable error rates per service and tracking against them, 10) Incident response runbook template for common error scenarios with diagnosis steps, resolution actions, and escalation path."
  },
  {
    "name": "caching-architect",
    "description": "Design multi-layer caching architecture for applications",
    "trigger": "caching strategy cache design",
    "instructions": "Design the multi-layer caching architecture. Provide: 1) Cache layer identification covering L1 in-process memory cache for single-instance data like configuration and lookup tables with sub-millisecond access, L2 distributed cache using Redis for shared data across multiple instances with millisecond access, L3 CDN cache for static assets and public API responses with global distribution, and L4 database query cache for expensive aggregations, 2) Cache key design strategy using hierarchical naming convention such as module:entity:id:version to prevent collisions and enable pattern-based invalidation, 3) TTL strategy per data type defining short TTL for volatile data like stock prices and session data, medium TTL for semi-static data like product catalog, and long TTL for static data like country list and currency codes, 4) Cache invalidation patterns covering time-based expiry for data that can tolerate brief staleness, event-driven invalidation where cache is cleared when underlying data changes, and versioned cache keys where cache key includes data version making stale keys naturally expire, 5) Cache-aside pattern implementation where application checks cache first and on miss fetches from database and populates cache, versus write-through where every write updates both database and cache simultaneously, 6) Redis data structure selection using String for simple key-value, Hash for objects to allow partial updates, List for queues and recent items, Set for unique collections, Sorted Set for leaderboards and time-ordered data, and choosing appropriately per use case, 7) Cache warming strategy for critical data that must be in cache at application start to prevent cold-start performance degradation, 8) Cache stampede prevention using locking or probabilistic early expiry to prevent multiple simultaneous requests rebuilding the same cache entry after expiry, 9) Cache monitoring metrics covering hit rate target above 80 percent, memory usage and eviction rate, cache operation latency, and key expiry rate, 10) ERP-specific caching candidates including chart of accounts tree, permission matrix per user role, exchange rates, tax codes, and configuration settings as high-value caching targets."
  },
  {
    "name": "realtime-architect",
    "description": "Design real-time features including WebSocket, notifications, and live updates",
    "trigger": "real-time websocket live updates notifications",
    "instructions": "Design the real-time features architecture. Provide: 1) Technology selection between WebSocket for bidirectional communication needed for chat and collaborative editing, Server-Sent Events for unidirectional server-to-client updates needed for dashboards and notifications, and Long Polling as fallback for environments that block WebSocket, with decision criteria for each, 2) WebSocket server architecture covering connection management, authentication of WebSocket connections using the same token as REST API validated on upgrade, heartbeat mechanism to detect stale connections, and reconnection strategy on the client side with exponential backoff, 3) Channel and subscription design where clients subscribe to specific topics such as user notification channel, document update channel, or dashboard metrics channel, receiving only relevant events, 4) Message format standard with fields for event type, payload, timestamp, and message ID for deduplication, 5) Horizontal scaling design for WebSocket servers using sticky sessions or shared pub-sub backend like Redis Pub-Sub so any server instance can deliver messages to any connected client, 6) Push notification architecture for mobile apps covering APNs for iOS and FCM for Android with device token management, notification payload format, delivery confirmation tracking, and notification preference management per user, 7) In-app notification center design with notifications stored in database for persistence, unread count badge, mark as read functionality, notification categories with different icons and actions, and pagination for notification history, 8) ERP-specific real-time events covering approval request received requiring action, document status changed such as purchase order approved, budget threshold exceeded warning, and task assigned notification, 9) Rate limiting for real-time events preventing a single event storm from overwhelming clients by batching rapid updates, 10) Offline message queuing ensuring notifications sent while user is disconnected are delivered when they reconnect."
  },
  {
    "name": "file-media-architect",
    "description": "Design file upload, storage, and media management architecture",
    "trigger": "file upload media storage documents",
    "instructions": "Design the file and media management architecture. Provide: 1) File storage strategy recommending object storage like S3 or compatible service over database storage for files above 1MB, with folder structure design using logical paths such as organization-id then module then year then month to enable lifecycle policies, 2) Chunked upload design for large files splitting file into chunks on client side, uploading each chunk with retry on failure, and assembling on server side, supporting resume after interruption with upload session tracking, 3) File metadata schema storing original filename, stored filename as UUID to prevent path traversal, file size, MIME type, upload timestamp, uploader user ID, module and record it is attached to, access level, and virus scan status, 4) Access control for files defining public files served directly through CDN, private files requiring signed URL with expiry time generated on each access request, and internal files accessible only to specific roles, 5) Signed URL generation for secure temporary access to private files with configurable expiry and optional IP restriction, 6) Virus scanning integration scanning every uploaded file before making it accessible, quarantining suspicious files, and notifying administrators, 7) Image processing pipeline covering automatic resize to multiple dimensions for thumbnails, format conversion to WebP for web display, metadata stripping to remove GPS and personal data from photos, and storing processed versions with original preserved, 8) ERP document management covering attachment of files to any ERP transaction, document versioning keeping history of uploaded versions, document approval workflow for critical documents, and document search by content using full-text extraction from PDF, 9) Storage lifecycle policy automatically moving files older than configured period to cheaper cold storage tier and deleting files beyond retention period, 10) File download architecture using pre-signed URLs to prevent hotlinking and ensure access control is enforced at every download not just on first access."
  }
]

output_root = "skills"

# تنظيف المجلد القديم بالكامل لإعادة البناء النظيف
if os.path.exists(output_root):
    shutil.rmtree(output_root)
os.makedirs(output_root, exist_ok=True)

def safe_yaml_string(text):
    """دالة مطورة لتنظيف النصوص بشكل كامل وحمايتها داخل الـ YAML من أخطاء الـ Parsing"""
    if not text:
        return '""'
    # إزالة أي سطور برمجية مكسورة أو مسافات زائدة
    cleaned = str(text).strip().replace('\\', '\\\\').replace('"', '\\"')
    return f'"{cleaned}"'

index_data = []

for skill in skills_data:
    # استخدام المعرف الآمن الخالي من المسافات
    safe_id = skill['name'].strip().lower().replace(" ", "-")
    skill_folder = os.path.join(output_root, safe_id)
    os.makedirs(skill_folder, exist_ok=True)
    
    name_val = safe_yaml_string(skill['name'])
    desc_val = safe_yaml_string(skill['description'])
    trigger_val = safe_yaml_string(skill['trigger'])
    
    # تنسيق القوائم الرقمية بشكل نظيف عبر السطور
    instructions_formatted = skill['instructions']
    if ". Provide:" in instructions_formatted:
        instructions_formatted = instructions_formatted.replace(". Provide:", ".\n\n### Requirements & Deliverables:\n")
    
    # البحث عن الأرقام الترتيبية مثل 1) و 2) و 10) ووضعها في سطر منفصل
    instructions_formatted = re.sub(r'(?:, | )(\d+\))', r'\n\1', instructions_formatted)

    # بناء هيكل الملف متطابقاً مع المعايير القياسية بدقة
    skill_md_content = f"""---
name: {name_val}
description: {desc_val}
trigger: {trigger_val}
---

# Role and Instructions
{instructions_formatted}
"""
    
    file_path = os.path.join(skill_folder, "SKILL.md")
    with open(file_path, 'w', encoding='utf-8', newline='\n') as md_file:
        md_file.write(skill_md_content)
    
    index_data.append({
        "id": safe_id,
        "path": f"skills/{safe_id}/SKILL.md"
    })

# توليد ملف الفهرس النظيف
with open("skills.json", 'w', encoding='utf-8') as json_file:
    json.dump(index_data, json_file, indent=2)

print("🛡️ تم إعادة التوليد الهندسي! الملفات الآن خالية من مشاكل التنسيق تماماً وجاهزة للرفع.")
